-- Create database for Manpower System
CREATE DATABASE IF NOT EXISTS manpower_db;

-- Use the database
USE manpower_db;

-- Note: Tables will be auto-created by Hibernate when you run the application
-- (because of spring.jpa.hibernate.ddl-auto=update in application.properties)

